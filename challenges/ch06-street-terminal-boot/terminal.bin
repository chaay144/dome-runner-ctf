(async () => {
  const s = "DOME_BOOT_OK_7F3A";        // exact text, no spaces
  const b = new TextEncoder().encode(s);
  const d = await crypto.subtle.digest("SHA-256", b);
  const hex = [...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('');
  console.log(hex);
})();
