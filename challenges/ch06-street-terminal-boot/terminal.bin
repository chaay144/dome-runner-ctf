(async () => {
  const flag = "flag{street_terminal_boot}";
  const enc = new TextEncoder().encode(flag);
  const buf = await crypto.subtle.digest("SHA-256", enc);
  const hex = [...new Uint8Array(buf)].map(b=>b.toString(16).padStart(2,'0')).join('');
  console.log(hex);
})();
