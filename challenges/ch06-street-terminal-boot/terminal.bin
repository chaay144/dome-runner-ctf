#include <stdio.h>
int main(){
  puts("Maintenance v0.1");
  const char *leak = "DOME_BOOT_OK_7F3A";  // <- the discoverable answer
  (void) leak;                              // keep it in .rodata
  puts("System OK");
  return 0;
}
